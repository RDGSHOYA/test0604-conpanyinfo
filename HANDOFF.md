# HANDOFF

## 1. アプリの説明

このアプリは、法人営業担当者が新規顧客への初回訪問前に使う「営業訪問前レポート」作成ツールです。ユーザーが「お客様企業名」「担当者名」「担当者の部署名」を入力すると、企業概要の仮説、部署ごとの想定課題、提案仮説、商談で確認すべき質問、上司レビュー用チェック項目、次のアクションを1つのレポートとして画面に表示します。外部検索やバックエンドは使わず、ブラウザ内の固定テンプレートから文章を生成する、依存関係なしの静的HTMLアプリです。

## 2. ローカルでの実行方法

依存関係のインストールは不要です。ローカルで確認するだけなら、ブラウザで `index.html` を直接開いてください。

配布用の `dist/index.html` を作る場合は、プロジェクトルートで次のコマンドを実行します。

```powershell
npm run build
```

ビルド後は、生成された `dist/index.html` をブラウザで開いて確認できます。

## 3. ファイル構造

- `BRIEF.md`: 元の仕様書です。ユーザー、目的、画面構成、必要機能、完成条件が書かれています。
- `index.html`: アプリ本体です。HTML、CSS、JavaScriptがすべてこの1ファイルに入っています。入力フォーム、レポート生成ロジック、レポート表示、コピー機能を含みます。
- `package.json`: npmスクリプト定義です。現在は `npm run build` だけを提供しています。外部依存関係はありません。
- `build.ps1`: `index.html` を `dist/index.html` にコピーするためのPowerShellビルドスクリプトです。
- `dist/index.html`: `npm run build` で生成された配布用HTMLです。
- `README.md`: 起動方法を1段落で説明した簡易READMEです。
- `HANDOFF.md`: この引き継ぎメモです。

## 4. 未完成な部分、ダミーになっている部分

- 実際の企業調査は行っていません。企業概要は、入力された企業名を使ったサンプル仮説として生成しています。
- 外部API、AI API、検索API、バックエンド、データベースは使っていません。
- 部署名ごとの課題・提案仮説は固定テンプレートです。
- 部署カテゴリは、入力された部署名に含まれるキーワードで簡易判定しています。現在の主なカテゴリは、情報システム、人事、経理・財務、営業、一般部門です。
- レポート作成日はブラウザ実行時の現在日付を使っています。
- コピー機能はブラウザのクリップボードAPIに依存しています。
- 入力内容や生成レポートは保存されません。ページをリロードすると初期状態に戻ります。

## 5. 既知のバグや制約

- `file://` で直接開いた場合、ブラウザや権限設定によってはクリップボードコピーが失敗することがあります。その場合は静的サーバーやデプロイ環境で確認してください。
- 実データに基づく企業情報ではないため、営業現場で使う前に企業Webサイトや公開情報で確認する必要があります。
- 部署名の判定は単純な文字列一致なので、表記ゆれや特殊な部署名では一般部門として扱われることがあります。
- すべてが `index.html` に入っているため、今後機能追加が増えると保守しにくくなります。
- in-app browserでの目視確認は接続タイムアウトにより完了していません。ビルドとJavaScript構文チェックは完了しています。

## 6. Webチームへの推奨される次のステップ

1. まず `index.html` をブラウザで開き、入力、レポート生成、リセット、コピーを手動確認してください。
2. 静的サイトとして配布する場合は `npm run build` を実行し、`dist/index.html` をNetlifyなどに配置してください。
3. 継続開発する場合は、HTML/CSS/JavaScriptを分割し、レポート生成ロジックを独立したモジュールに移すことを推奨します。
4. 部署カテゴリとテンプレート文面を営業チームとレビューし、実際の商談準備に合う粒度へ調整してください。
5. 将来的にAI APIや検索APIへ差し替える場合は、現在の固定テンプレート生成部分をAPI呼び出し用の関数に置き換えてください。
6. 本番利用前に、PCとスマートフォンの主要ブラウザで表示崩れ、コピー機能、印刷表示を確認してください。
